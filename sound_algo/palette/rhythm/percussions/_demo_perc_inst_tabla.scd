// =========================================================
//  block -- extrait de percussions.scd
// =========================================================
( ~percInst = \tabla;   ~reload.value; "perc = tabla (authentic)".postln; )
